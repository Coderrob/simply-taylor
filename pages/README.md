## Pages

This folder exists to match Squarespace's expected template structure.

Use `/pages/` for static page templates such as:

- `[name].page`
- `[name].page.conf`

For the documented Squarespace template file structure, see:
https://developers.squarespace.com/template-overview
